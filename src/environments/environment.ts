import type { Environment } from './environment.type';

export const environment: Environment = {
  production: true,
  apiUrl: 'https://api.amainlevee.app',
  socketUrl: 'https://api.amainlevee.app',
  defaultFormId: ''
};
